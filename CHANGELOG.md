# 更新日志

mpvKt 的所有重要变更都记录在此。格式参考
[Keep a Changelog](https://keepachangelog.com/);每个版本段落的标题必须包含
发布 tag 名(例如 `## v0.2.0`),发行工作流会把对应段落提取到 GitHub
发行说明里。

## v0.2.1 - 2026-09-01

### 修复

- **修复 release / preview 构建启动播放器必然崩溃的问题**:R8
  会把 mpv 通过 JNI 回调的私有 `eventProperty` 方法裁剪掉,导致
  mpv 初始化时以 `NoSuchMethodError` 中止;现在完整保留
  mpv-android-lib 的类成员
- **修复"选择文件"播放时默认不选中音轨的问题**:
  - 持久化逐视频轨道状态时不再把未知轨道 id(mpv 空闲时报告为
    "auto")落库,恢复时也会跳过历史遗留的无效值,不再把音轨 /
    字幕强制写成"关闭"
  - 修复 `track-list` 属性的初始空事件导致确定性轨道选择被一次性
    跳过的竞态,现在会等待真实轨道列表就绪后再选择
- 修正 18 种语言中解码器格式化字符串的非位置参数资源警告
- 消除全部编译弃用警告:迁移新版 `AnchoredDraggable` API,音频
  焦点统一为 compat API,剪贴板迁移到 `LocalClipboard`,画中画改
  用非弃用重载等

### 新增

- 首次启动时引导授予"所有文件访问"权限,并附带用途说明

## v0.2.0 - 2026-09-01

### 播放核心

- 基于重写的 **mpv-android-lib 0.1.13**:与上游 mpv-android master
  对齐,fontconfig 字体发现(自动生成 fonts.conf)、全新的
  ffmpeg / libplacebo 依赖栈,原生库以 API 23 编译
  (**最低支持版本提升至 Android 6.0**)
- 修复 mpv.conf / input.conf 不再生效的问题(配置目录恢复为在
  mpv 初始化之前设置)

### 播放体验

- 确定性的默认轨道选择:音轨始终默认第一条;字幕按
  偏好语言 → 内嵌字幕 → 外挂字幕 的优先级选择
  (已保存的逐视频轨道状态仍然优先)
- 截图按钮恢复为纯点击,旁边新增"上一帧 / 下一帧"按钮,
  每按一次显示当前时间位置
- 截图保存后自动登记到 MediaStore,现在能正常出现在相册中
- 修复画中画状态下旋转屏幕导致的崩溃

## v0.1.6 - 2026-08

- 最后一个基于 mpv-android-lib 0.1.12 的版本
- 截图功能重做:独立"带字幕 / 不带字幕"按钮、逐帧截图、
  自定义保存目录
- 18 种语言与应用内语言切换
